1. 登录和启动更新界面新增“当前版本 vX.X.X”显示；下载新版本时可同时区分当前版本与目标版本。
2. 移除主界面“切换事业部”按钮及无用的前端、preload 和主进程 IPC 调用链；需要退出时继续使用右上角关闭按钮。
3. 快速打标“单品SKU”新增“SKU前N个”：在时间、价格和商品状态过滤后，按每个 SPU 分别取前 N 个 SKU；不足 N 个时保留该 SPU 的全部剩余 SKU，并记住上次设置。
4. 保留差分更新失败识别、差分基础文件预检和完整安装包断点续传兜底；启动页及运行中更新弹窗继续显示更新模式、百分比、速度、预计时间和累计更新内容。
5. 保留商家端登录窗口优化：登录与滑块、拼图、验证码等安全验证页面正常显示，进入商家工作台后立即转为后台隐藏窗口。
6. 保留商家端多账号隔离、登录凭据安全保存和会话恢复；店铺账号继续支持 Cookie 在线检测、重复账号覆盖及账号密码自动填写。
7. 保留快速打标的商品时间、价格、状态、单品 SKU 筛选、采集进度及桌面 TXT 导出。
8. 保留 WMS 多仓库登录恢复：仅在同时取得完整仓库名称和编号后保存 Cookie 并隐藏窗口；重启后验证待验收单据，网络异常不误删 Cookie。
9. v1.0.66 用户继续先通过 v1.0.66.3 等长桥接补丁升级到 v1.0.68，再由完整更新流程升级到本版本；v1.0.67～v1.0.75 支持直接升级。
10. 本版本提供完整安装包、blockmap 和 latest.yml；差分不可用、旧安装缓存缺失或校验失败时自动切换为完整安装包。
