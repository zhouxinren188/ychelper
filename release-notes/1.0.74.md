1. 修复差分更新失败后进度重新开始却仍显示“差分更新”的问题：现在能识别 electron-updater 内部回退，并明确切换为“完整更新”。
2. 增加差分基础文件预检：本机缺少或清理了旧版 installer.exe 时直接下载可断点续传的完整安装包，不再先执行一次注定失败的差分下载。
3. 优化更新界面：启动更新页的进度条固定为紧凑横线，避免被纵向布局拉伸；启动页和运行中更新弹窗均显示更新模式、百分比、速度、预计时间和更新内容。
4. 保留商家端登录窗口优化：登录与安全验证页面正常显示，进入商家工作台后立即转为后台隐藏窗口，避免登录成功后短暂看到工作台界面。
5. 保留安全验证兜底：检测到滑块、拼图、验证码或独立安全验证页面时自动显示并聚焦窗口；身份接口连续验证失败时重新显示。
6. 主界面的账号入口保持为“切换事业部”，并继续支持商家端多账号隔离、登录凭据安全保存和会话恢复。
7. 保留店铺 Cookie 状态检测、快速打标、商品时间/价格/SKU 筛选、采集进度、TXT 导出、WMS 登录恢复、仓库信息校验、自动验收和本地脱敏日志等功能改进。
8. 正式版本继续提供完整安装包、blockmap 和 latest.yml；支持 1.0.68 等长期未启动版本直接升级到当前最新版，差分不可用时自动回退完整包并校验大小与 SHA-512。
