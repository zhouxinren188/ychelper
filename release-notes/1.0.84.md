1. 修复完整更新包下载并校验完成后，云仓助手能够退出但安装器未启动，导致再次打开仍停留在旧版本并重复升级的问题。
2. 完整安装包现在先释放当前在线名额，再使用 NSIS 更新参数创建可验证的安装进程；只有确认安装器已经启动后，云仓助手才会退出。
3. 安装器启动时若遇到 Windows 临时文件占用，会在当前程序仍可显示结果的阶段自动重试；连续重试仍失败时明确提示，不再静默结束升级流程。
4. 继续保留安装包等待旧程序退出的保护，并保持原有 appId、安装目录和用户数据目录不变；升级不会主动清除 WMS Cookie、账号或业务配置。
5. 本版本为完整安装包修复版本，提供安装包、blockmap 和 latest.yml。v1.0.67 及以后清单内受支持版本可直接升级到 v1.0.84；v1.0.66 继续通过既有兼容桥接，最终升级到 v1.0.84。
