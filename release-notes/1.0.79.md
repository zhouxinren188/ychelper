1. 加固快速打标查询链路：查询期间锁定店铺账号与结果状态，避免切换、删除或重新登录导致会话串号；登录失效、业务风控和普通网络异常分别处理，不再误清有效 Cookie。
2. 修复商品分页、SKU 展开和结果组合：严格按照服务器实际分页大小继续查询，缺少商品编码时独立分组，SKU 编号去空去重，异常价格不再显示为 NaN。
3. 价格范围改为严格数字筛选；设置价格条件后自动排除无有效价格的 SKU，最低价高于最高价时阻止查询并给出明确提示。
4. “单品SKU”的全部、SKU第1个、SKU最后1个、最低价、最高价、SKU前N个和随机N个均按每个 SPU 独立计算；筛选后不足 N 个时保留该 SPU 的全部剩余 SKU。
5. 重做快速打标商品列表：按 SPU 分组展示商品主图、官方标题、价格范围、状态和操作日期；展开后显示 SKU 规格与编号，并增加已选数量、半选状态、空结果提示和更清晰的层级样式。
6. 商品状态优先读取京东商品响应；明确查询售卖中或已下架时可使用筛选条件回退，全部商品缺少实际状态字段时显示“未知”，避免误标。
7. 保留店铺账号与 Cookie 状态管理、重复账号覆盖、凭据安全保存、自动填写、桌面 TXT 导出及发送到打标/下标等现有功能。
8. 保留 WMS 多仓库名称和编号识别、有效 Cookie 恢复、待验收单据查询及网络异常不误删 Cookie；保留商家端验证码页面显示和登录成功后隐藏工作台。
9. 保留启动前自动检查、运行中定时检查、差分下载、完整包断点续传兜底、下载速度、预计时间和中文更新说明；账号、Cookie 与业务配置继续保存在稳定用户目录。
10. 本版本为完整安装包版本，提供安装包、blockmap 和 latest.yml。v1.0.66 用户继续通过 v1.0.66.3 与 v1.0.68 兼容桥接升级；v1.0.67 及以后受支持版本可直接升级到本版本。
