1. 新增商家端账号切换入口：在主界面可安全返回账号登录页，选择历史账号或登录新账号，切换时停止旧授权会话并重建当前账号业务数据。
2. 修复商家端账号隔离：切换账号时完整覆盖用户资料、CSRF Token、sellerId 和 Cookie，避免沿用上一账号缓存；商家端身份接口未验证通过时不再误判登录成功。
3. 修复登录界面与账号保存：“记住用户名和密码”现在真正生效，补充空值、长度、重复提交及登录窗口单实例保护；删除未使用的旧登录代码。
4. 加固本地数据：账号配置和 Cookie 不再在系统安全存储异常时降级为明文，无法解密时禁止覆盖原数据，并使用临时文件原子写入；新增可跨多版本执行的数据结构迁移。
5. 修复店铺账号恢复：登录成功后同步保存最后使用的店铺 ID 和真实店铺名称，账号切换、重复账号覆盖及删除清理保持一致，避免重启后店铺 ID 与名称错配。
6. 修复 WMS 会话恢复：必须同时具备完整仓库名称和编号才标记已登录；缺失时自动打开页面重新识别；账号保存、排重、删除和自动填充统一使用当前账号。
7. 为授权服务器和 WMS 验证请求增加主动超时，网络异常时保留有效 Cookie 并允许重试，避免启动或查询长期卡住。
8. 保留此前快速打标、店铺商品时间/价格/SKU 筛选、采集进度、WMS 自动验收、本地脱敏日志以及旧版本直接升级最新版等功能改进。
