1. 修复完整安装包更新时旧版云仓助手尚未彻底退出、安装器随即提示“云仓助手无法关闭”并反复重试的问题。新版本会先启动独立等待器，确认主进程及同路径子进程全部退出后再打开已经完成大小和 SHA-512 校验的安装包。
2. 为从 v1.0.81 等旧客户端发起的完整包更新增加安装器兼容等待：安装器检查文件占用前最多等待 10 秒，让旧客户端有时间完成退出；等待有明确上限，手动启动安装包且软件仍在使用时仍会保留原有安全提示，不会无条件强制结束业务进程。
3. 统一并压缩订阅窗口和应用内弹窗的自绘标题栏，移除订阅窗口的原生系统标题栏，减少顶部多余留白，同时保留标题、拖动、最小化和关闭操作。
4. 精简订阅窗口顶部信息，只展示当前事业部和订阅状态；隐藏右侧滚动条轨道但保留滚轮、触控板和键盘滚动，减少界面视觉干扰。
5. 本版本为完整安装包版本，提供安装包、blockmap 和 latest.yml。v1.0.67 及以后清单内受支持版本直接升级到 v1.0.82，不要求先安装 v1.0.81；v1.0.66 继续通过既有 v1.0.66.3 与 v1.0.68 兼容桥接，最终升级到 v1.0.82。账号、Cookie 和业务配置继续保存在稳定用户目录。
