# AGENTS.md - AI 开发规范备忘

## 测试与发布授权

1. **本机测试为主**：开发者主要在本机进行功能测试。每次修改代码后如需人工操作、界面交互或真实业务环境测试，由开发者自行在本机完成。

2. **允许的自动检查**：可以在不发布、不部署的前提下运行语法检查、单元测试、静态检查和只读诊断；不得把自动检查等同于发布授权。

3. **禁止擅自发布**：开发者未明确提出“发布新版本”时，不得未经同意擅自修改版本号、生成发布安装包或热更新包、上传更新文件、部署服务器、重启线上服务或以其他方式向用户分发代码。

4. **发布操作必须明确授权**：只有开发者明确要求发布后，才可以执行对应的打包、上传和部署步骤。修复代码、提交代码或要求测试，均不代表已经授权发布。

## 更新包发布规范

1. **热更新适用范围**：热更新只允许更新 `src/` 渲染层文件。热更新包必须包含当前版本完整的 `src/` 目录，禁止只打本次改动的增量文件，确保用户可以从任意旧热更新版本直接升级。

2. **必须发布完整安装包的情况**：修改 `main.js`、`preload.js`、`package.json`、构建脚本或 Electron 主进程逻辑，新增、删除或升级 npm 依赖，以及其他需要更新 `app.asar`、`main.jsc` 或 `node_modules` 的改动，必须使用 `electron-builder` 重新构建完整安装包，不能使用热更新分发。

3. **ZIP 目录结构**：热更新 ZIP 中只能包含 `src/`，并保持与应用根目录一致的目录结构（如 `src/css/style.css`、`src/js/renderer.js`），解压后覆盖应用的 `app.asar.unpacked/src/`。

4. **发布前自行判断包类型**：获得明确发布授权后，应根据实际改动自动判断使用热更新还是完整安装包；不要把技术判断重新交给开发者，但任何判断都不得绕过上一节的发布授权要求。

## 服务器部署信息

- SSH: `ssh -o BatchMode=yes Administrator@150.158.54.108`
- SCP: `scp -o BatchMode=yes <local> Administrator@150.158.54.108:"<remote>"`
- 服务器项目路径: `C:\ychelper-server`
- PM2 进程名: `ychelper`
- 管理后台密码：通过安全渠道获取，不得写入代码库或开发规范。
- 上传热更新: `curl -s -X POST -H "x-admin-password: <admin-password>" -F "version=X.X.X" -F "changelog=..." -F "file=@update-X.X.X.zip" http://150.158.54.108:3000/api/update/upload`
