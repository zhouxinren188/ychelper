# AGENTS.md - AI 开发规范备忘

## 测试与发布授权

1. **本机测试为主**：开发者主要在本机进行功能测试。每次修改代码后如需人工操作、界面交互或真实业务环境测试，由开发者自行在本机完成。

2. **允许的自动检查**：可以在不发布客户端的前提下运行语法检查、单元测试、静态检查和只读诊断；不得把自动检查等同于客户端发布授权。

3. **禁止擅自发布客户端**：开发者未明确提出“发布新版本”时，不得未经同意擅自修改版本号、生成发布安装包或热更新包、上传更新文件、切换 `latest.yml` 或以其他方式向用户分发客户端代码。

4. **客户端发布必须明确授权**：只有开发者明确要求发布新版本后，才可以执行客户端改版号、打包、上传和更新指针切换。修复代码、提交代码、测试或服务器部署均不代表已经授权发布客户端。

5. **服务器修复可直接部署**：服务器代码修改通过适用测试后，可按既有安全部署规范直接部署，无需逐次询问。部署前必须建立精确回滚备份，保留 `.env`、数据库及运行数据，只重启实际受影响的服务并完成健康检查；不得借机重启或修改无关服务。遇到会影响旧客户端、无法安全备份回滚、健康检查失败或需要业务决策时必须停止并报告。

## 更新包发布规范

1. **正式版本强制全量**：所有三段正式版本号 `X.Y.Z` 都必须发布完整安装包、blockmap 和 `latest.yml`，不得只发布热更新。热更新仅用于当前完整版本上的紧急渲染层修订，版本号必须使用 `X.Y.Z.N`，且 `X.Y.Z` 必须与服务器当前最新版完整安装包完全一致。

2. **热更新适用范围**：热更新只允许更新 `src/` 渲染层文件。热更新包必须包含当前版本完整的 `src/` 目录，禁止只打本次改动的增量文件，确保用户可以从同一完整基线下的任意旧热更新修订直接升级。服务器必须拒绝三段热更新版本、错误完整基线及缺少完整安装包或 blockmap 的热更新上传。

3. **必须发布完整安装包的情况**：修改 `main.js`、`preload.js`、`package.json`、构建脚本或 Electron 主进程逻辑，新增、删除或升级 npm 依赖，以及其他需要更新 `app.asar`、`main.jsc` 或 `node_modules` 的改动，必须使用 `electron-builder` 重新构建完整安装包，不能使用热更新分发。

4. **ZIP 目录结构**：热更新 ZIP 中只能包含 `src/`，并保持与应用根目录一致的目录结构（如 `src/css/style.css`、`src/js/renderer.js`），解压后覆盖应用的 `app.asar.unpacked/src/`。

5. **ASAR 解包文件仍有长度约束**：`asarUnpack` 只把文件内容放到 `app.asar.unpacked`，`app.asar` 头仍记录文件长度和完整性信息。热更新打包必须读取同一完整基线的真实 ASAR 元数据；登录页必须注入热修订号后补齐到基线记录的精确字节数，并通过 ASAR 实际读取测试。任何渲染文件超过基线记录长度时不得强行热更新，必须改发新的完整版本。位于 `src/` 但由 `main.js` 直接 `require` 的模块仍属于主进程逻辑，修改时必须发布完整版本。

6. **长期未启动用户的固定顺序**：只要服务器存在基于最新版完整包的四段热更新，旧用户启动时必须先升级到该完整版本并重启，之后才能获得对应热更新。热更新检查不得把基于较新完整版本的 ZIP 下发给较旧基础版本；无法证明这个顺序时，必须停止热更新发布并改发新的三段完整版本。

7. **发布前自行判断包类型**：获得明确发布授权后，应根据实际改动自动判断使用四段紧急热修订还是新的三段完整版本；正式功能发布一律使用完整版本。任何判断都不得绕过上一节的发布授权要求。

## 完整安装包与自动更新强制规范

1. **已发布版本不可变**：版本号一旦上线，禁止重新构建、覆盖或替换同版本的安装包、blockmap、元数据和更新内容。任何修订都必须提升版本号，以免客户端缓存、SHA-512 和服务器文件互相冲突。

2. **发布三件套缺一不可**：完整版本必须同时生成并发布 `latest.yml`、`ychelper-setup-X.X.X.exe`、`ychelper-setup-X.X.X.exe.blockmap`。发布前先把新完整版本加入 `release-baselines.json`，再运行 `npm run verify:release-artifacts -- --version=<新版本>`；校验器默认读取该清单，检查版本号、文件名、大小、SHA-512、blockmap 结构和全部承诺支持的历史基线。任一项失败不得发布。

3. **发布顺序必须安全**：先上传并验证新版本 `.blockmap`，再上传完整安装包和完整更新元数据，最后才允许让 `latest.yml` 指向新版本。严禁在 blockmap 尚未可访问时提前暴露新 `latest.yml`，避免所有客户端差分失败后回退全量下载。

   直接部署服务器文件时必须使用 `scripts/deploy-full-release-metadata.ps1` 分两阶段执行：第一次不带 `-Publish`，只生成并解析 pending 元数据；确认输出 `PENDING_METADATA_OK` 后，第二次才带 `-Publish` 原子替换线上指针。禁止再用临时 PowerShell 字符串直接覆盖 `latest.yml`。

4. **保留历史文件并明确支持边界**：`release-baselines.json` 是承诺支持自动升级的唯一清单，只有服务器同时保留完整安装包和对应 `.blockmap` 的版本才允许写入。服务器不得在发布新版本时删除清单内文件；本地 `dist` 中遗留的安装包不能自动视为线上基线。历史 `.blockmap` 必须通过与 `latest.yml` 中安装包相同的端点访问，并正确支持 `HEAD`、单段 `Range` 和 `multipart/byteranges` 多段请求。

5. **直接跨版本升级**：除下文明确记录的 v1.0.66 兼容桥接外，自动更新必须支持“任意受支持旧版本 → 服务器最新版本”直接升级，即使用户连续错过十几个版本，也不得要求依次安装中间版本或由开发者手动发包。`latest.yml` 永远只指向服务器最新版；差分不可用、跨度过大或缓存不完整时必须自动切换到最新版完整包，支持断点续传、大小和 SHA-512 校验，并自动安装重启。

6. **安装包必须自包含**：每个完整安装包都必须包含当时所需的全部代码、资源和依赖，不得依赖某个中间版本已经安装过。用户数据保存在稳定的 Electron `userData` 目录，升级不得改变 appId、清空用户资料、登录账号或 Cookie。

7. **数据迁移必须可跨多版补齐**：涉及本地数据结构的变更必须使用独立、递增的 schema 版本或唯一迁移编号；所有历史迁移长期保留，按版本顺序执行并保证幂等。禁止只处理“上一版本 → 当前版本”，也禁止复用一个布尔标记跳过后来新增的迁移。

8. **启动更新顺序**：应用启动后先显示专用“正在检查更新”状态，账号登录表单暂不显示。确认没有完整更新和热更新后才显示登录；发现更新后停留在更新状态，自动下载、校验、安装和重启，不再要求用户点击确认。

9. **更新进度信息**：下载界面必须显示版本号、百分比、下载速度、预计剩余时间和本次更新内容。检查或下载失败不能造成白屏、并发启动两套下载或永久阻止用户登录；完整包临时网络失败须保留有效的部分文件供下次续传。

10. **运行中检查**：已启动的软件至少每 30 分钟检查一次完整版本更新。发现新版后使用与启动时相同的单飞下载流程，禁止同一版本重复下载或差分与完整包并发下载。

11. **固定构建运行时**：Electron 等会改变安装包绝大多数二进制块的核心构建依赖必须使用精确版本，禁止使用会自动跨版本漂移的范围。升级 Electron 大版本必须单独评估兼容性、安装包体积和现网最老版本的差分比例；不得把运行时大版本升级混入普通业务发布，导致差分更新退化为接近完整包。

## 发布验收清单

1. 发布前先确认已获得明确授权，并核对 `release-baselines.json`。清单必须包含新版本、v1.0.66 和服务器上所有仍承诺支持且同时具备 exe/blockmap 的版本；不得凭本地残留文件猜测，也不得为通过校验随意删除真实活跃基线。

2. 完整构建必须通过语法检查、全部单元测试、更新流程契约测试及发布文件三件套校验。涉及 `main.js`、`preload.js`、依赖或构建脚本时不得改用热更新规避完整构建。

3. 上线时必须写入 UTF-8 中文更新内容，且更新内容应涵盖从现网最老版本到本次最新版之间用户实际获得的累计重要变更，不能只写最后一个中间版本的变化。

   Windows PowerShell 5.1 禁止把中文更新内容直接作为 `curl.exe -F "changelog=..."` 的命令行参数，原生程序参数会按旧代码页转换并把乱码永久写入服务器元数据。必须使用 UTF-8 安全方式上传（例如 Node.js `fetch`/`FormData` 直接传 Unicode 字符串，或先写入无 BOM UTF-8 文件再让上传工具读取文件内容）。上传后必须重新调用更新检查接口，逐字比较返回的 `changelog` 与原文；只校验版本号、ZIP 大小和 SHA-512 不算发布成功。若乱码元数据已经曝光，禁止覆盖同版本，必须提升四段热修订号或正式版本号重新发布。

4. 上线后必须运行 `npm run verify:online-release -- --version=<最新版>`。正式验收默认读取 `release-baselines.json`，禁止用临时手写的 `--baselines` 缩小范围来宣称成功；显式参数只用于故障诊断。必须确认：`latest.yml` 只指向当前最新版；每个基线的 exe/blockmap 均为 HTTP 200；单段 Range 和多段 `multipart/byteranges` 均为 HTTP 206；每个老版本都能获得正确的升级元数据。v1.0.66 必须额外验证 1.0.66.3 ZIP、13,791 字节限制和 v1.0.68 桥接。

5. 必须使用独立 `userData` 测试目录完成一次“现网最老版本 → 最新版”的真实启动升级冒烟测试，确认检查、下载、校验、安装、重启以及账号/Cookie/业务配置迁移正常。禁止修改开发者日常使用的 `config.json` 来伪造更新状态；没有完成真实跨版本升级时，不得只凭接口返回宣称成功。

6. 任何发布步骤失败都应停止后续曝光；不得删除或覆盖已经上线的旧文件。修复后使用新的版本号重新构建发布。

7. 登录页、更新页等使用 `contextBridge.exposeInMainWorld()` 的页面必须通过真实打包后的 Electron/Chromium 冒烟测试。页面脚本禁止用 `const`、`let` 或 `class` 再声明与 preload 注入对象相同的全局名称（例如 `electronAPI`）；Chromium 会因不可配置全局属性发生语法错误，导致整段脚本不执行。JSDOM 模拟测试不能单独作为通过依据；至少应验证版本号显示、已保存账号密码恢复、登录按钮事件和渲染进程无语法异常。

## 2026-08-11 v1.0.77.1 登录页脚本失败复盘

1. **现象**：用户从 v1.0.76.1 完整升级到 v1.0.77、再应用 v1.0.77.1 后，登录页仍只显示“当前版本”，保存过的账号和密码也没有回填。
2. **根因**：登录页声明了 `const electronAPI = window.electronAPI || {}`，而 preload 已通过 `contextBridge` 注入不可配置的同名全局对象。真实 Chromium 抛出 `SyntaxError: Identifier 'electronAPI' has already been declared`，导致包含版本显示、凭据恢复和登录按钮绑定的整段内联脚本完全不执行。账号数据并未因此被删除。
3. **测试遗漏**：原有 JSDOM 测试只给 `window.electronAPI` 赋普通可配置属性，无法复现 Electron `contextBridge` 的全局属性约束；即使 ASAR 长度、ZIP 哈希和 JSDOM DOM 断言都通过，也不能证明真实 Electron 页面能够解析执行。
4. **修复与验收**：页面内部改用不冲突的局部变量名，并增加同名全局词法声明静态拦截；随后必须从真实打包程序启动登录页，通过 Chromium DevTools 协议确认版本号、保存凭据、强制重载后账号密码恢复以及无 `Runtime.exceptionThrown`。

## 1.0.66 旧客户端兼容桥接

1. **不得宣称 1.0.66 可直接升级**：原始 v1.0.66 登录页缺少完整包下载完成事件监听，且其 `full-check` 曾被服务端禁用。仅验证 `latest.yml`、安装包和 blockmap 可访问，不能证明该版本能够完成安装。
2. **固定升级链**：仍在使用 v1.0.66 的用户必须先通过 `/api/update/check?version=1.0.66` 获得 `1.0.66.3` 渲染层补丁。该补丁必须在登录页显示兼容升级状态，并在旧主进程下载完完整安装包后定时调用 `confirmUpdateInstallByPath()`，不能只依赖可能丢失的 `show-update-install` 事件；完整包兜底固定返回 v1.0.68。安装并重启 v1.0.68 后，再按正常完整更新流程升级到服务器最新版。
3. **旧版 ASAR 长度约束**：v1.0.66 的 `src/login.html` 在 ASAR 头中固定为 13,791 字节。桥接补丁中的登录页必须从原始 v1.0.66 安装包提取、在脚本最前面注入兜底，并压缩、填充到恰好 13,791 字节；禁止直接使用更长页面覆盖，否则 Electron 会按旧头长度截断 JavaScript。发布与线上验收都必须检查该精确字节数。
4. **长期保留桥接文件**：服务器必须长期保留历史 `update-1.0.66.1.zip`、`update-1.0.66.2.zip`、当前 `update-1.0.66.3.zip`、v1.0.68 安装包及其 blockmap；所有已发布桥接包均不得覆盖。只要后台仍存在 v1.0.66 活跃用户，不得删除、替换或停止该兼容链。
5. **发布验收必须包含 v1.0.66**：`release-baselines.json` 必须长期包含 v1.0.66。线上校验必须实际检查 1.0.66.3 ZIP 的大小/SHA-512、登录页 13,791 字节、自动安装兜底、v1.0.68 桥接元数据与文件，以及 v1.0.68 到最新版的正常升级路径。
6. **桥接状态持续到 full-check**：v1.0.66.3 可能重复请求 `latest.yml`，也可能在首次请求后被用户关闭。服务器必须在兼容会话有效期内持续返回 v1.0.66 保持元数据，直至该客户端成功进入 `full-check`；进入后立即释放会话，避免同一公网 IP 下的其他客户端被旧元数据影响。带 `verify` 的通用发布探测不得被残留桥接会话污染，专用 `legacy-bridge` 验收仍必须走真实桥接逻辑。
7. **旧版更新接口禁止304**：v1.0.66 使用 `net.request` 读取 `/api/update/check`、`/api/update/download` 和 `/api/update/full-check` 的完整正文，不能可靠处理无正文的HTTP 304。上述动态更新接口必须忽略 `If-None-Match`/`If-Modified-Since` 条件头，返回 `Cache-Control: no-store`，并在发布验收中携带缓存条件重复请求，确认始终返回HTTP 200及完整正文。

## 2026-08-11 v1.0.76 与 v1.0.66.3 发布复盘

1. **本次正式版本功能**：v1.0.76 在登录和更新界面增加当前软件版本号，移除主界面无必要的“切换事业部”入口，并为快速打标增加按每个 SPU 取前 N 个 SKU 的筛选；同时累计交付店铺账号/Cookie 状态管理与重复账号覆盖、商品时间/价格/状态/单品 SKU 筛选、采集进度及桌面 TXT 导出；完善 WMS 多仓库名称和编号识别、Cookie 恢复、待验收查询与网络异常保留；完善商家端多账号隔离、凭据安全保存、登录后隐藏工作台及验证码重新显示；更新流程增加启动前检查、紧凑进度条、模式/速度/预计时间/更新内容、差分基础预检和完整包断点续传兜底。
2. **旧版兼容链**：v1.0.66 先获取四段桥接补丁 v1.0.66.3；补丁恢复可见更新状态并轮询旧主进程的完整包安装接口，随后下载、校验并安装 v1.0.68，再由正常更新流程升级到当前最新版。服务器同时恢复旧接口禁用缓存、重复 `latest.yml` 保持、完整包元数据和历史 blockmap 验收。
3. **两次失败的根因**：v1.0.66 的 `src/login.html` 虽然位于 `app.asar.unpacked`，Electron 仍按 `app.asar` 头部记录的 13,791 字节读取。v1.0.66.1 和 v1.0.66.2 写入了更长页面，运行时脚本在中间被硬截断，造成登录按钮和更新兜底同时失效。磁盘文件存在、版本号正确、ZIP 校验通过都不能证明页面实际完整执行。
4. **最终修复方式**：必须从原始 v1.0.66 安装包提取登录页，在第一个内联脚本开头注入最小兜底，再压缩页面外壳并填充到恰好 13,791 字节。生成器、单元测试和线上 ZIP 解包验收三处都必须检查该长度和 `legacy-full-installer-fallback-v3` 标记。
5. **真实验收结论**：本机已实际完成 v1.0.66 → v1.0.66.3 → v1.0.68 的下载、安装和重启，用户随后再次从 v1.0.66 验证成功。今后不得把接口 200、文件哈希正确或软件出现下载提示单独当作升级成功。
6. **历史 v1.0.68 的已知行为**：v1.0.68 下载当前最新版后仍可能显示一次“立即安装”确认，这是已发布历史版本的行为，不能覆盖原安装包修改。若以后要求该链路完全无人值守，必须发布新的桥接修订或新的完整版本路径，并重新做最老版本实机冒烟测试。
7. **配置文件编码陷阱**：Windows PowerShell 旧版 `Set-Content -Encoding UTF8` 可能写入 BOM，旧客户端会把 `config.json` 判为损坏并丢失桥接标记。测试工具写 JSON 必须使用无 BOM UTF-8，并先备份；更推荐使用隔离 `userData`，禁止直接改生产用户资料。
8. **失败后的版本处理**：已上传的 v1.0.66.1、v1.0.66.2、v1.0.66.3 都是不可变发布物。任何后续修正必须使用 v1.0.66.4 或新的三段完整版本；不得因旧包失败而覆盖同名 ZIP、安装包、blockmap 或元数据。
9. **验收基线管理**：本次曾因手工把只有安装包、没有 blockmap 的 v1.0.63/v1.0.64 加入验收而失败。以后只能由 `release-baselines.json` 定义支持范围；新完整版本发布时加入清单，旧版本退出支持必须先确认无活跃用户并获得明确授权。
10. **元数据必须先离线解析**：v1.0.75 首次发布时，临时 PowerShell 数组拼接把 `latest.yml` 的 URL 和 SHA-512 错拆到下一行，正式线上验收随即拦截并回滚到 v1.0.74。以后生成待发布 `latest.yml` 后，必须在切换前用与客户端/验收脚本相同的规则解析并逐项核对 version、path、size、sha512；禁止只目测文件或只校验安装包。元数据一旦曝光后发现错误，必须回滚指针并提升正式版本号，不得覆盖同版本修复。

## 服务器部署信息

- SSH: `ssh -o BatchMode=yes Administrator@150.158.54.108`
- SCP: `scp -o BatchMode=yes <local> Administrator@150.158.54.108:"<remote>"`
- 服务器项目路径: `C:\ychelper-server`
- 更新接口监听端口: `3000`
- Windows 服务名: `YchelperServer`，显示名 `YcHelper Server`，由 `C:\nssm\nssm.exe` 管理。部署 `routes/`、`services/` 或其他服务器运行代码后，使用 `Restart-Service -Name "YchelperServer" -Force` 重启，再确认服务状态为 `Running` 且 `Get-NetTCPConnection -LocalPort 3000 -State Listen` 存在监听进程。
- 远程 SSH 默认命令解析环境可能是 `cmd.exe`。包含 PowerShell 管道、`$_` 或多层引号的命令应先按 UTF-16LE 编码，再用 `powershell -NoProfile -EncodedCommand <base64>` 执行，避免引号被远程 shell 截断后误判服务不存在。
- 同机 PM2 中的 `ant-toolbox-server`、`dianxiaoer-update` 以及 NSSM 的 `dianxiaoer-*` 均与云仓助手更新服务无关，禁止为发布云仓助手而重启或删除这些进程。
- 服务重启后必须重新运行 `npm run verify:online-release -- --version=<最新版>`，由 `release-baselines.json` 提供完整范围；仅看到服务为 `Running` 不能视为部署成功。
- 管理后台密码：通过安全渠道获取，不得写入代码库或开发规范。
- 上传热更新: `curl -s -X POST -H "x-admin-password: <admin-password>" -F "version=X.Y.Z.N" -F "changelog=..." -F "file=@update-X.Y.Z.N.zip" http://150.158.54.108:3000/api/update/upload`。其中 `X.Y.Z` 必须等于服务器当前完整版本。
